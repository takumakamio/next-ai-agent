# プロンプト集

> 研修中に Claude Code へ入力するプロンプト（指示文）をステップ順にまとめています。
> コピー＆ペーストしてそのまま使えます。

---

## Step 0：Claude Code の使い方を覚えよう

### ウォーミングアップ — Lv.1 質問する

> 「Next.js って何ですか？初心者向けに教えて」

### ウォーミングアップ — Lv.2 ファイルを作る

> 「このフォルダに hello.ts というファイルを作って。Hello World を表示するプログラムを書いて」

### ウォーミングアップ — Lv.3 実行する

> 「今作った hello.ts を実行して」

### Next.js プロジェクトの初期化

> 「Next.js のプロジェクトを初期化して。TypeScript と Tailwind CSS 4 を使う構成で。パッケージマネージャーは npm で。」

### 環境変数の設定

> 「.env.local ファイルを作って。中身はこの2つ：
> DATABASE_URL=postgresql://...（Neon の接続文字列）
> GOOGLE_GENERATIVE_AI_API_KEY=（自分の API キーをここに貼る）」

---

## Step 1：データベースを作ろう

### Drizzle ORM の導入

> 「Drizzle ORM を導入して。PostgreSQL 用の設定もお願い。drizzle.config.ts も作って。データベースの接続設定は db/database.ts に書いて」

### Q&A テーブルの作成

> 「Q&A を保存するテーブルを作って。カラムは：id（nanoid で自動生成）、question（質問文）、answer（回答文）、category（カテゴリ）、embedding（2000次元のベクトル、pgvector 使用）、createdAt、updatedAt。db/schema/qas.ts に作って」

### マイグレーションスクリプトの追加

> 「マイグレーションファイルを生成して実行するスクリプトを package.json に追加して。db:generate と db:migrate と、両方を連続で実行する db:gnm も作って」

### シードデータの投入

> 「シードスクリプトを db/seed.ts に作って。プログラミングに関する Q&A を5件くらい入れたい。例えば『TypeScript の型とは？』みたいな初心者向けの質問。package.json に db:seed スクリプトも追加して」

### データ確認

> 「DB にデータが入っているか確認するスクリプトを書いて実行して」

---

## Step 2：API を作ろう

### Hono のセットアップ

> 「Hono を Next.js の App Router で使いたい。app/api/[[...route]]/route.ts にセットアップして」

### Q&A 一覧取得 API

> 「Q&A の一覧を取得する GET API を作って。ページネーション対応で、検索もできるようにして。features/qas/ フォルダにルートとリポジトリを分けて配置して」

### Q&A 新規作成 API

> 「Q&A を新規作成する POST API を作って。Zod でバリデーションして。question、answer、category は必須にして」

### API の動作確認

> 「今作った API の動作確認を curl コマンドで試したい。一覧取得と新規作成の curl コマンドを教えて」

---

## Step 3：AI と会話できるようにしよう

### Google AI クライアントのセットアップ

> 「@google/genai パッケージを使って、Gemini のクライアントを設定するユーティリティファイルを lib/google-ai.ts に作って。embedding 生成の関数も用意して。モデルは gemini-embedding-001、次元数は 2000 で。API キーは環境変数から読み込んで」

### ベクトル検索の実装

> 「ユーザーの質問を embedding に変換して、DB に保存されている Q&A とコサイン類似度で検索する関数を作って。類似度 0.65 以上のものだけ返して」

### 会話 API の実装

> 「チャット用の POST API を features/home/routes/conversation.ts に作って。ユーザーの質問を受け取って、ベクトル検索で関連 Q&A を見つけて、それをコンテキストとして Gemini 2.5-flash に渡して回答を生成して。会話履歴（history）も受け取って、前の会話を踏まえた回答ができるようにして」

---

## Step 4：画面を作ろう

### shadcn/ui の導入

> 「shadcn/ui を導入して。Button、Input、Card、Dialog、Table、Textarea、Tabs コンポーネントを追加して」

### チャット画面の作成

> 「チャット画面を作って。シンプルにメッセージリストと入力欄だけ。ユーザーのメッセージは右側、AI の回答は左側に表示。送信したら conversation API を呼んで回答を表示して。ダークモードのかっこいいデザインで」

### Q&A 管理画面の作成

> 「Q&A の管理画面を作って。一覧表示はテーブル形式で、新規追加のフォームも付けて。テーブルには ID、カテゴリ、質問、作成日を表示。フォームには質問、回答、カテゴリの入力欄を用意して」

### タブ切り替えの追加

> 「トップページにタブを付けて、『チャット』と『Q&A 管理』を切り替えられるようにして」

---

## Step 5：仕上げ & 振り返り — カスタマイズ例

> 「ローディング中にスピナーを表示して」

> 「チャットの吹き出しのデザインをもっとかわいくして」

> 「Q&A のカテゴリをバッジで色分けして」

> 「送信ボタンにアイコンを付けて」

> 「エラーが起きたときにトースト通知を出して」

> 「チャット履歴をクリアするボタンを付けて」

> 「Q&A の削除ボタンを付けて」

> 「メッセージ送信時に Enter キーで送れるようにして」

> 「全体のフォントサイズをもう少し大きくして」

> 「余白をもう少し広くして」

> 「ヘッダーにアプリ名を表示して」

---

## Step 6：実践チャレンジ — チャット要約機能

### 要約 API の作成（ヒント）

> 「チャットの会話履歴を要約する POST API を features/home/routes/summary.ts に作って。リクエストで messages（role と content の配列）を受け取って、Gemini に『以下の会話を日本語で3行以内で要約して』と送って、結果を返して。既存の features/home/routes/conversation.ts を参考にして。routes/index.ts にも登録して」

### 要約ボタンの追加（ヒント）

> 「チャット画面に『要約する』ボタンを追加して。会話が2件以上あるときだけ表示して。ボタンを押したら POST /api/home/summary に会話履歴を送って、返ってきた要約をチャット画面の下部にカード形式で表示して」

### ローディング・再要約（ヒント）

> 「要約ボタンを押している間はローディング表示にして。要約が完了したらボタンのテキストを『再要約』に変えて」

---

## 研修後チャレンジ（時間が余った人向け）

> 「チャットの質問と回答をログとして DB に保存する機能を追加して」

> 「Q&A を編集できるフォームを追加して」

> 「Gemini の TTS と STT を使って音声でチャットできるようにして」

> 「CSV ファイルから Q&A を一括登録できる機能を作って」

> 「Three.js と @pixiv/three-vrm で VRM アバターを表示して、リップシンクもつけて」

> 「AI の回答をストリーミングで1文字ずつ表示して」
